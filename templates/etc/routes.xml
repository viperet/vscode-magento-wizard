<?xml version="1.0"?>
<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="urn:magento:framework:App/etc/routes.xsd">
    <router id="standard">
        <route id="${1:route_id}" frontName="${2:route}">
            <module name="{{ vendor }}_{{ extension }}" />
        </route>
    </router>
</config>